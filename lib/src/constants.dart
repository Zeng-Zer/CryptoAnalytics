import 'package:flutter/material.dart';

const sideMargin = EdgeInsets.symmetric(horizontal: 16.0);
const innerSpacing = EdgeInsets.all(8.0);
const circularBorder = BorderRadius.all(Radius.circular(12));

// Graphs / Charts
const graphInnerSpacing = EdgeInsets.fromLTRB(8, 14, 8, 0);
const graphHeight = 300.0;
